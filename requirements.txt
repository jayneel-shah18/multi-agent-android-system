# Multi-Agent Android Automation System Dependencies
# Core Python packages for the automation pipeline

# Data processing and analysis
numpy>=1.21.0
pandas>=1.3.0
scikit-learn>=1.0.0

# Optional: Android environment (will fall back to MockEnv if not available)
# android_env  # Uncomment if you have access to android_env

# Development and testing (optional)
pytest>=6.0.0
black>=21.0.0

# Note: The system is designed to work with minimal dependencies
# and will gracefully fall back to mock implementations if
# optional packages are not available.

# For enhanced features (future):
# opencv-python>=4.5.0  # For visual analysis
# tensorflow>=2.8.0     # For ML-based improvements
# langchain>=0.1.0      # For LLM integration
